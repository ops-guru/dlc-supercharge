"""Integration tests for the assembled DLC bridge (Epic 2b).

Tests in this package exercise multiple modules together with mocked
external dependencies (notably ``subprocess.run`` standing in for the
real ``claude`` CLI). Pure-unit tests live in ``tests/unit/`` instead.
"""
